import { PluginSettings } from './PluginSettings';

export const DEFAULT_SETTINGS: PluginSettings = {
	apiToken: null,
	workspace: { id: 'none', name: 'None selected' },
	charLimitStatusBar: 40,
	activeTimerCheckInterval: 1000
};
